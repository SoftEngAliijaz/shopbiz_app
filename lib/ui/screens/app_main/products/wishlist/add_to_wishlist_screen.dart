import 'package:flutter/material.dart';

class AddToWishlistScreen extends StatelessWidget {
  const AddToWishlistScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AddToWishlistScreen'),
      ),
      body: Center(
        child: Text('AddToWishlistScreen'),
      ),
    );
  }
}
